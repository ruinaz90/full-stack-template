const mongoose = require('mongoose')

/*const someSchema = new mongoose.Schema({
  text: {
    type: String,
    required: true,
  },
  completed: {
    type: Boolean,
    required: true,
  }
})

module.exports = mongoose.model('nameOfDB', someSchema)*/